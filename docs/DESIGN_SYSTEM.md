# Sanmu AI 视觉系统设计规范

## 🎨 设计哲学

**深藏功名，极简外露**

Sanmu AI 的视觉系统遵循「极简主义」设计哲学，通过克制的配色、精细的线条和充足的留白，传递专业、沉稳、高冷的品牌气质。

---

## 🌲 配色系统

### 主色调：森林深绿

```css
/* 主色 - 森林深绿 */
--primary-color: #2D5F3F;
--primary-dark: #1F4529;
--primary-light: #3D7F5F;

/* 辅助色 - 冷淡灰 */
--secondary-color: #6B7280;
--secondary-dark: #4B5563;
--secondary-light: #9CA3AF;

/* 背景色 */
--background-primary: #FFFFFF;
--background-secondary: #F9FAFB;
--background-tertiary: #F3F4F6;

/* 文字色 */
--text-primary: #1F2937;
--text-secondary: #6B7280;
--text-tertiary: #9CA3AF;
--text-inverse: #FFFFFF;

/* 边框色 */
--border-light: #E5E7EB;
--border-medium: #D1D5DB;
--border-dark: #9CA3AF;

/* 功能色 */
--success: #10B981;
--warning: #F59E0B;
--error: #EF4444;
--info: #3B82F6;
```

### 配色原则

1. **80/20 原则**：80% 留白 + 20% 视觉元素
2. **低饱和度**：避免使用高饱和度颜色，保持克制
3. **高对比度**：文字与背景对比度 ≥ 4.5:1（WCAG AA 标准）
4. **层次感**：通过灰度变化区分信息层级

---

## 📐 布局系统

### 间距规范

```css
/* 间距系统（8px 基准） */
--spacing-xs: 8rpx;   /* 4px */
--spacing-sm: 16rpx;  /* 8px */
--spacing-md: 24rpx;  /* 12px */
--spacing-lg: 32rpx;  /* 16px */
--spacing-xl: 48rpx;  /* 24px */
--spacing-2xl: 64rpx; /* 32px */
--spacing-3xl: 96rpx; /* 48px */
```

### 圆角规范

```css
/* 圆角系统 */
--radius-sm: 8rpx;   /* 小元素：标签、徽章 */
--radius-md: 16rpx;  /* 中等元素：按钮、输入框 */
--radius-lg: 24rpx;  /* 大元素：卡片 */
--radius-xl: 48rpx;  /* 特大元素：圆形按钮 */
--radius-full: 9999rpx; /* 完全圆形 */
```

### 留白设计

- **页面边距**：32rpx（16px）
- **卡片内边距**：32rpx（16px）
- **元素间距**：24rpx（12px）
- **段落间距**：48rpx（24px）

**目标**：界面保持 **80% 留白**，仅 20% 用于内容展示

---

## 🔤 字体系统

### 字体家族

```css
/* 系统字体栈 */
font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", 
             "PingFang SC", "Hiragino Sans GB", 
             "Microsoft YaHei", sans-serif;
```

### 字号规范

```css
/* 字号系统 */
--font-xs: 20rpx;   /* 10px - 辅助文字 */
--font-sm: 24rpx;   /* 12px - 次要文字 */
--font-base: 28rpx; /* 14px - 正文 */
--font-lg: 32rpx;   /* 16px - 小标题 */
--font-xl: 36rpx;   /* 18px - 标题 */
--font-2xl: 40rpx;  /* 20px - 大标题 */
--font-3xl: 48rpx;  /* 24px - 特大标题 */
```

### 字重规范

```css
/* 字重系统 */
--font-weight-normal: 400;  /* 正文 */
--font-weight-medium: 500;  /* 强调 */
--font-weight-semibold: 600; /* 小标题 */
--font-weight-bold: 700;    /* 标题 */
```

### 行高规范

```css
/* 行高系统 */
--line-height-tight: 1.2;   /* 标题 */
--line-height-normal: 1.5;  /* 正文 */
--line-height-relaxed: 1.75; /* 长文本 */
```

---

## 🎯 图标系统

### 图标风格

**极细线条（0.5pt Outline）**

- **线宽**：0.5pt（极细线条）
- **风格**：几何、简约、对称
- **尺寸**：24rpx × 24rpx（标准）、32rpx × 32rpx（大）

### 图标使用原则

1. **一致性**：同一页面使用相同风格的图标
2. **语义化**：图标含义清晰，无需文字说明
3. **留白**：图标周围保持 8rpx 以上间距
4. **对齐**：图标与文字垂直居中对齐

### 图标库推荐

- **Lucide Icons**（极简线条风格）
- **Feather Icons**（轻量级）
- **Heroicons**（Outline 版本）

---

## 🖼️ 卡片系统

### 卡片样式

```css
/* 标准卡片 */
.card {
  background: #FFFFFF;
  border-radius: 24rpx;
  padding: 32rpx;
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.04);
}

/* 悬浮卡片 */
.card-elevated {
  box-shadow: 0 8rpx 32rpx rgba(0, 0, 0, 0.08);
}

/* 边框卡片 */
.card-outlined {
  border: 2rpx solid #E5E7EB;
  box-shadow: none;
}
```

### 卡片使用场景

- **信息展示**：用户信息、会员卡片
- **功能入口**：功能卡片、快捷操作
- **内容容器**：列表项、表单

---

## 🔘 按钮系统

### 按钮样式

```css
/* 主按钮 */
.btn-primary {
  background: linear-gradient(135deg, #2D5F3F 0%, #1F4529 100%);
  color: #FFFFFF;
  border-radius: 48rpx;
  padding: 24rpx 48rpx;
  font-weight: 600;
}

/* 次要按钮 */
.btn-secondary {
  background: #FFFFFF;
  color: #2D5F3F;
  border: 2rpx solid #2D5F3F;
  border-radius: 48rpx;
  padding: 24rpx 48rpx;
}

/* 文字按钮 */
.btn-text {
  background: transparent;
  color: #2D5F3F;
  padding: 16rpx 24rpx;
}
```

### 按钮尺寸

```css
/* 按钮尺寸 */
--btn-height-sm: 64rpx;  /* 小按钮 */
--btn-height-md: 88rpx;  /* 标准按钮 */
--btn-height-lg: 96rpx;  /* 大按钮 */
```

---

## 📊 数据可视化

### 图表配色

```css
/* 图表主色 */
--chart-primary: #2D5F3F;
--chart-secondary: #6B7280;
--chart-accent: #10B981;

/* 图表渐变 */
--chart-gradient-start: rgba(45, 95, 63, 0.3);
--chart-gradient-end: rgba(45, 95, 63, 0);
```

### 图表样式

- **线条宽度**：2-3rpx
- **网格颜色**：#E5E7EB
- **背景色**：透明或 #F9FAFB
- **圆角**：16rpx

---

## 🎭 动效系统

### 过渡时间

```css
/* 过渡时间 */
--transition-fast: 150ms;    /* 快速：悬停、点击 */
--transition-base: 300ms;    /* 标准：展开、收起 */
--transition-slow: 500ms;    /* 慢速：页面切换 */
```

### 缓动函数

```css
/* 缓动函数 */
--ease-in: cubic-bezier(0.4, 0, 1, 1);
--ease-out: cubic-bezier(0, 0, 0.2, 1);
--ease-in-out: cubic-bezier(0.4, 0, 0.2, 1);
```

### 动效原则

1. **克制**：避免过度动效，保持专业感
2. **流畅**：60fps 流畅度
3. **有意义**：动效应传递信息，而非装饰

---

## 🖼️ 图片处理

### 图片比例

- **机位照片**：16:9
- **滤镜缩略图**：1:1
- **头像**：1:1
- **横幅**：21:9

### 图片优化

- **格式**：WebP（优先）、JPEG
- **压缩**：TinyPNG、ImageOptim
- **尺寸**：根据显示尺寸提供 1x、2x、3x 版本

---

## 📱 响应式设计

### 断点系统

```css
/* 断点 */
--breakpoint-sm: 375px;  /* 小屏手机 */
--breakpoint-md: 414px;  /* 标准手机 */
--breakpoint-lg: 768px;  /* 平板 */
```

### 适配原则

1. **移动优先**：先设计小屏，再扩展到大屏
2. **弹性布局**：使用 flex、grid
3. **相对单位**：使用 rpx、%、vh/vw

---

## ✅ 设计检查清单

### 配色检查
- [ ] 主色使用森林深绿 (#2D5F3F)
- [ ] 避免使用高饱和度颜色
- [ ] 文字对比度 ≥ 4.5:1

### 布局检查
- [ ] 页面留白 ≥ 80%
- [ ] 间距符合 8px 基准
- [ ] 圆角统一（8/16/24/48rpx）

### 字体检查
- [ ] 字号符合规范（20-48rpx）
- [ ] 行高合理（1.2-1.75）
- [ ] 字重适当（400-700）

### 图标检查
- [ ] 使用极细线条（0.5pt）
- [ ] 图标大小一致（24/32rpx）
- [ ] 图标与文字对齐

### 交互检查
- [ ] 按钮高度 ≥ 88rpx
- [ ] 点击区域 ≥ 44rpx × 44rpx
- [ ] 过渡时间 ≤ 300ms

---

## 🎨 设计资源

### Figma 设计稿
- **主文件**：Sanmu AI Design System
- **组件库**：Sanmu Components
- **图标库**：Sanmu Icons

### 字体资源
- **PingFang SC**（苹果系统默认）
- **Microsoft YaHei**（Windows 系统默认）

### 配色工具
- **Coolors**：配色方案生成
- **Contrast Checker**：对比度检查

---

© 2025 Sanmu AI. All rights reserved.
